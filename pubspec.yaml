name: w
description: "A Flutter package that adds a fun dab animation to your widgets."
version: 1.0.0
repository: https://github.com/wdestroier/w

environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
